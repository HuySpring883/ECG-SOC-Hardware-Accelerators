// squared.h
#ifndef SQUARED_H
#define SQUARED_H

#define BUFFSIZE 600
extern int squared[BUFFSIZE];  // Declare squared as an external variable

void squareFunc(int length, int derivative);  // Function prototype

#endif
